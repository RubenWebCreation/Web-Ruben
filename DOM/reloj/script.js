//optener todos los enlances
out = document.links;

const enlaces = document.links
for (let link of enlaces) {
    console.log(link.href);
    
}
//
out document.images
const imagenes = document.images
for(let imagen of imagenes){
    console.log(imagen.src);
    
}
//
const imgArray